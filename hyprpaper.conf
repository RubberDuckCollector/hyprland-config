# the , would have something before it specifying what screen the bg should be on
# if there's nothing there, it applies the image to all screens
wallpaper {
    monitor =
    # path = ~/Pictures/windows_xp_14.jpg # bliss
    path = ~/Pictures/windows_xp_10.jpg # desert island
    fit_mode = cover
}
