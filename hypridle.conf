general {
    lock_cmd = pidof hyprlock || hyprlock       # avoid starting multiple hyprlock instances.
}


listener {
    timeout = 100
    on-timeout = notify-send "Timeout in 200 seconds."
}

listener {
    timeout = 200
    on-timeout = notify-send "Timeout in 100 seconds."
}

listener {
    timeout = 250
    on-timeout = notify-send "Timeout in 50 seconds."
}

listener {
    timeout = 290
    on-timeout = notify-send "Timeout in 10 seconds."
}

listener {
    timeout = 300
    on-timeout = loginctl lock-session
}
